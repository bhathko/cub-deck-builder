# figma-to-code 主題分享 — 簡報大綱與講稿

> - **講者身份**:專案開發者
> - **聽眾**:UIUX 設計師為主、少數工程師
> - **時長**:60–90 分鐘(下方每節標時間;趕時間時的刪節法在最後「時間伸縮指南」)
> - **基調**:深入淺出。講「我們做了什麼、踩過什麼坑、哪些模式可以帶走」,不炫技、不隱惡。
> - 文中所有數字都是 2026-08-07 從 repo 實測或 worklog 原文取得,來源附在附錄。

---

## 簡報總覽(時間配置)

| # | 節 | 時間 | 一句話 |
|---|---|---|---|
| 1 | 開場:我們想解決什麼 | 8 分 | 「PM/UIUX 要的是整頁,不是殘稿」 |
| 2 | 現場 Demo:一條 Figma 連結到一整頁 | 12 分 | 眼見為憑,先看結果再講原理 |
| 3 | 系統怎麼運作(深入淺出) | 15 分 | 產生程式碼的過程,一個 AI 都沒有 |
| 4 | 我們走過的彎路與痛點 | 15 分 | 從 10 支 AI 接力,到把 AI 全部請出產線 |
| 5 | 可以帶走的模式 | 10 分 | 不猜、留洞、找裁判、建閉環 |
| 6 | 設計稿要怎麼畫,系統才吃得下 | 12 分 | 給設計師的實用段:規則其實比想像少 |
| 7 | 五個大哉問(事先收集的問題) | 12 分 | 能就說能,不能就說不能和為什麼 |
| 8 | 未來方向 + 現場 Q&A | 6 分+ | 還缺什麼、往哪走 |

---

# 第 1 節 開場:我們想解決什麼(8 分)

## 投影片要點

- 標題頁:**「figma-to-code:把設計稿變成一整頁能跑的程式」**
- 一張對比圖:左邊 Figma frame 截圖 → 右邊瀏覽器裡跑起來的同一頁
- 問題陳述三行:
  1. 設計稿畫完之後,到「看得到、點得動的頁面」中間隔著什麼?
  2. 市面上的 AI 生成工具為什麼常常「看起來對、其實錯」?
  3. 我們的目標使用者是 PM/UIUX——他們要的是**整頁**,不是「之後找工程師補」的半成品。

## 講稿

大家好,今天要分享的是我們做的 figma-to-code 系統:貼一條 Figma 連結進去,吐出一整頁真的能跑的 Angular 頁面。

先講清楚我們的出發點,因為它決定了後面所有的技術選擇。這個工具的目標使用者不是工程師,是 PM 和 UIUX。這句話的意思是:產出來的東西必須是**一整頁完整可跑的頁面**——表單長在該長的地方、下拉選單點了會展開、表格有表頭有分頁。如果產出來是一堆殘缺的程式碼片段,說「剩下的找工程師補」,那對 PM 和設計師來說等於沒有。

第二個出發點,是我們對「AI 生成程式碼」的態度。大家應該都試過各種 AI 生頁面的工具,體感通常是:第一眼很驚豔,細看開始不對勁——元件用錯、資料是編的、每次重生都長得不一樣。我們一開始也是走 AI 生成的路,而且走得很深,後來整條路砍掉重來。這段故事是今天的重點之一,等一下第 4 節會完整講。

先講結論,今天整場只要記住一句話:

> **這套系統產生程式碼的過程,一個 AI 都沒有。唯一會叫 AI 的地方,是編譯失敗之後找它來修。**

聽起來很反直覺對吧?一個 2026 年的新專案,居然把 AI 從核心請出去。為什麼這樣做、換到了什麼,就是今天要講的。

---

# 第 2 節 現場 Demo:一條 Figma 連結到一整頁(12 分)

## 投影片要點

- 只放一頁投影片:「接下來 10 分鐘沒有投影片,看真的」
- Demo 順序(事先排練,挑一張規範內的設計稿):
  1. 在產頁台貼 Figma 網址 → 按「先檢查設計稿」→ 看健檢結果(分組問題 + 可點的 Figma 深連結)
  2. 執行產生 → 看進度串流
  3. (如果該頁有資料洞)停在「等待填寫」→ 展示 manifest 表單:每個洞寫著「這一欄接哪個資料來源」,很多欄位已經被設計稿的樣本值預填
  4. 送出 → `ng build` 編譯通過 → 打開頁面:點下拉選單、開日期選擇器、按鈕開出彈窗
  5. 最後給大家看一眼產出的程式碼:乾淨的 Angular、用的全是團隊元件庫的元件
- 保險:預先錄好一段同流程的螢幕錄影,現場網路或環境出狀況就播錄影

## 講稿

口說無憑,先看真的。(操作)

我貼一條 Figma 連結。先不急著產生——注意這顆「**先檢查設計稿**」按鈕。它會在不產生任何東西的情況下,把這張稿健檢一遍:哪裡有被拆散(detach)的元件、哪裡用了已淘汰的元件、彈窗的結構齊不齊。每個問題附一條可以直接點回 Figma 的連結。設計師可以自己先跑這個,不用等產生失敗才知道稿有問題。

現在真的產生。(執行)注意看進度:抓設計稿、解析、產碼、然後停下來了——它在問我問題。這就是我們說的「**資料層 manifest**」:設計稿畫得出「長相」,畫不出「資料」。表格裡那十列資料是設計師填的示意,不是真的;這一欄背後要接哪個資料來源,Figma 裡根本沒有這個資訊。遇到這種缺口,系統**不猜**,它停下來開一張表單問人。而且你看,大部分欄位已經預填好了——選項是從設計稿裡採收來的,之前回答過的問題也不會再問第二次。

填完送出,系統跑真正的 `ng build`——不是「看起來沒問題」的表面檢查,是真的 Angular 編譯。通過了,開頁面。(操作)下拉選單點開是真的會展開,日期選擇器點日曆真的會出面板,這顆按鈕在 Figma 上有拉 prototype 連線到一個彈窗——你看,點下去彈窗真的開了。

最後看一眼程式碼。(展示)這不是為了長得像而硬拼出來的一堆空方塊,是用我們元件庫元件寫出來的 Angular,跟工程師手寫的樣子一致。同一張設計稿丟進去一百次,產出**一模一樣**——這件事等一下會解釋為什麼重要、以及為什麼很難。

---

# 第 3 節 系統怎麼運作(深入淺出)(15 分)

## 投影片要點

**投影片 3-1:整條生產線(一張圖)**

```
抓設計稿 → 展開連結(這條連結會變成幾頁?)
  → 認元件(key 對照,不看名字)→ 蓋中間結構樹(0 無聲丟棄)
  → 逐元件照食譜(recipe)產碼 → 寫檔 + 自動接路由/選單
  → (有資料洞就停下問人:manifest)
  → ng build 當裁判(沒通過才叫 AI 來修,最多 10 輪)
  → 無頭瀏覽器真的開一次頁面(截圖 + 收錯誤訊息)
```

> 想現場走一遍「一個元件是怎麼被轉換的」→ 附錄 D 有可直接上投影片的四段對照(Figma 原始 JSON → 查身分 → 中間結構樹 → Angular 碼)。

**投影片 3-2:三個關鍵詞**

| 關鍵詞 | 白話 |
|---|---|
| **key 對照** | 認元件只看 Figma 元件的身分證字號(key),完全不看名字。名字會改、會打錯,key 不會。 |
| **recipe(食譜)** | 每種元件一份「怎麼從設計稿參數煮出 Angular 碼」的固定作法。目前 29 份,涵蓋表單、表格、卡片、頁籤、日期選擇器等。 |
| **manifest(資料洞)** | 設計稿沒有的資訊(資料來源、按鈕行為),留一個結構化的洞問人,絕不編造。 |

**投影片 3-3:認不得的東西怎麼辦(三色佔位框)**

- 灰色虛線框 = 未識別元件 → 產生後進「未識別元件」收件匣,一鍵對應,下次就認得
- 琥珀色框 = 已淘汰元件 → 請設計師換成現役版
- 藍色框 = 已交辦待開發 → 設計師不用做任何事,工程師補完自動消失

**投影片 3-4:鐵律「0 無聲丟棄」——每個圖層都要有下落**

- 定義:**無聲丟棄 = 設計稿上有、產出裡沒有,而且沒有告訴任何人**
- 為什麼它比破版更危險:破版看得見,看得見就有人修;消失的東西沒人找,還會讓畫面「看起來更乾淨」,一路活到驗收
- 規則:每個圖層進系統都要點名,合法的下落只有四種——

| 下落 | 意思 | 帳上 | 畫面上 |
|---|---|---|---|
| 轉成元件 | 認得,照食譜煮 | 有記錄 | 看得到元件 |
| 轉成版面 | 容器、排版結構 | 有記錄 | 看得到排版 |
| 登記「忽略」 | 純裝飾圖形、隱藏圖層 | 有記錄 | **看不到(刻意)** |
| 佔位框 + 待辦 | 認不得 | 有記錄 | 看得到誠實的框 |

- **不存在第五種「就這樣不見了」**——進來 100 個圖層,帳上就要有 100 個去向;帳不平是系統自己的 bug,當場失敗,不會硬吐一個缺件的頁面
- 但書:「忽略」帳上有、畫面上沒有 → 驗收完整度要看**覆蓋率報告**,不是數畫面上的框

**投影片 3-5:規模數字**

- 29 份元件食譜、85 筆元件登記、348 筆元件身分對照
- 後端自動測試 1380 條全數通過
- 一次產生的中位數時間:**16 秒**(核心產碼本身只要零點零幾秒,時間都花在編譯)

## 講稿

剛剛看到的流程,拆開來是這樣。(投影片 3-1)

第一步抓設計稿,跟 Figma 的伺服器把整棵設計結構抓回來。第二步很有趣,叫「展開連結」:你貼的可能是一張 frame、也可能是一整個 section,系統先算清楚「這條連結會變成幾頁」,而且**預告用的和真正產生用的是同一段程式**,所以預告跟結果永遠對得上。

第三步是整套系統的靈魂:**認元件**。這裡我們踩過大坑。Figma 裡每個元件有名字,也有一個唯一的 key,像身分證字號。早期我們試過用名字認——「這個圖層叫 select,應該是下拉選單吧」——結果災難連連:名字會被改、會打錯、會撞名。舉個真實數字:我們曾經用名字判斷「已淘汰元件」,38,021 個淘汰元件的實例裡,名字比對只抓到 **13 個**,命中率 0.03%。所以現在整套系統**只認 key,完全不看名字**。設計師用元件庫拉出來的元件天生帶 key;人做過的判斷也釘在 key 上,同一個元件最多只被問一次,之後改名都不影響。

認出來之後,每種元件走一份「**食譜**」——固定的、寫死的轉換規則:下拉選單怎麼產、表格的排序和分頁怎麼接、必填欄位怎麼掛驗證。目前 29 份。注意,食譜是程式,不是 AI——同樣的輸入永遠產出同樣的程式碼,這就是「同一張稿丟一百次結果一模一樣」的原因。

那認不得的呢?這是我們跟其他工具最大的差別:**不猜**。認不得的元件,畫面上留一個灰色虛線佔位框,寫著元件名,同時進一個收件匣。你在收件匣裡告訴系統「這其實是某某元件」,做一次,下次全部的頁面都認得。我們把這叫「**不認得 → 看得見 → 人決定 → 下次就認得**」的閉環。錯的對應是「自信的垃圾」,比誠實的佔位框更糟——這句是我們的原話,寫在工作日誌裡。

「不猜」的背後還有一條更底層的鐵律,值得單獨講一分鐘,它叫「**0 無聲丟棄**」。先定義:無聲丟棄,就是設計稿上有的東西,產出裡沒有,而且**系統沒有告訴任何人**。重點在「無聲」兩個字。大家直覺上可能覺得「破版」是最糟的錯,其實不是——破版看得見,看得見就有人去修。真正危險的是**消失**:沒有人會拿設計稿逐格比對產出,而且少了一塊之後,畫面往往反而「看起來更乾淨」,這種錯會一路活到驗收才被發現。先破個梗:我們曾經有一整張表格從產出裡無聲蒸發,編譯照樣全部通過、零錯誤訊息——完整故事留到下一節講。

所以系統立了這條鐵律:設計稿的**每一個圖層**進來都要點名,出去都要有下落,而且合法的下落只有四種——認得,轉成元件;是版面結構,轉成容器排版;純裝飾圖形或隱藏圖層,明確登記「忽略」,注意是**登記**,帳上有記錄;認不得,給誠實的佔位框、進待辦清單。**不存在第五種「就這樣不見了」。** 進來一百個圖層,帳上就要有一百個去向;哪個圖層沒有去向,那是系統自己的 bug,它會當場失敗,而不是硬吐一個缺了東西的頁面給你。可以把它想成記帳:每一筆錢都要有科目,「不明支出」是不被允許的。

這裡補一個誠實的但書,對之後驗收很實用:第三種「忽略」——像純裝飾的圖形——帳上有記錄,但畫面上刻意什麼都不留。所以判斷一頁轉得完不完整,**不要用「畫面上有沒有佔位框」來數**,要看系統吐出來的覆蓋率報告,那才是帳本。

最後,誰說了算?**`ng build` 說了算**。整條流程的終點是跑一次真正的 Angular 編譯,編得過才算數。編不過——這才是全流程唯一請 AI 出場的地方——AI 讀錯誤訊息修一輪,再編,最多十輪。而且我們還多做一層:編譯通過了不代表畫面是對的,所以最後用無頭瀏覽器(沒有視窗、由程式操控的瀏覽器)真的把頁面開起來,截圖、收集瀏覽器回報的錯誤訊息。這是被真實事故教出來的,第 4 節講。

---

# 第 4 節 我們走過的彎路與痛點(15 分)

## 投影片要點

**投影片 4-1:時間軸(一張圖)**

```
2026-06-17  第一版:10 支 AI 分工接力產一頁
2026-06-24  第一次醒悟:把「認元件」從 AI 手上拿走
2026-06-27  整條 AI 生成鏈退役(commit e6d901c)
2026-07-04  unmapped 閉環 + 黃金規則 + 迴歸網
2026-07-15  第二條路:「實作」功能(產完之後用白話請 AI 改碼)
2026-08-05  內網 Windows 安裝包(設計師機器免工程環境)
```

**投影片 4-2:為什麼放棄 AI 生成鏈(當時的判斷,原文)**

> 「產出不穩的根源,是讓 AI 猜『畫面上這個元件是元件庫裡的哪一個』。把識別交給確定性程式,對不上的明確標記,**不靜默、不猜**。」

- 真實災情:AI 猜版面插槽,一頁猜出 **36 個**(正確是 1 個)
- 家裡用 Claude、公司用 Codex,兩邊產出不一致
- 編譯修復迴圈裡每輪都開一支 AI,燒錢燒時間
- 「零判斷」的步驟(跑編譯、拼檔案)也在用 AI 做

**投影片 4-3:痛點精選(講 3–4 個故事)**

1. **通過編譯,畫面卻是壞的**——表單容器裡放了不合格的控制元件,編譯全部通過,頁面一開就直接報錯
2. **無聲消失**——整張表格從產出裡蒸發,沒有任何錯誤訊息
3. **有產出 ≠ 有畫面**——時間軸元件產出來了,初始資料是空陣列,畫面一片空白,而系統回報「一切正常」
4. **自信的捏造**——曾發現被寫死的按鈕文字「取消/確認」,對照設計稿,真實按鈕是「林小可」+「前往待辦案件」

## 講稿

現在講今天最重要的一段:我們是怎麼從「全 AI」走到「零 AI 產碼」的。

2026 年 6 月中,第一版系統是 **10 支 AI 分工接力**:一支讀設計稿、一支決定版面骨架、三支分頭產表單/版面/表格的程式片段、一支拼裝、一支產最終程式碼、一支審查、還有編譯檢查和修錯的。聽起來很先進,對吧?當時我們也這麼覺得。

實際跑起來的體感是三個字:**不穩定**。同一張稿,今天產的跟明天產的不一樣;家裡用 Claude 跑、公司用 Codex 跑,兩邊又不一樣。最經典的一次:AI 負責猜版面上的「插槽」位置,正確答案是 1 個,它猜了 **36 個**,整頁炸掉。而且我們發現連「跑一次編譯、把結果寫進檔案」這種**零判斷**的工作也在開 AI 做——純粹在燒錢。

6 月 24 日我們做了第一個關鍵切割:**把「認元件」從 AI 手上拿走**,改用剛剛講的 key 對照。效果立竿見影。三天後,6 月 27 日,我們把整條 AI 生成鏈砍掉——那個 commit 的訊息今天還查得到:「產生器不再用 AI 猜未覆蓋的元件,改為留下編譯安全的記號」。從那天起,產碼零 AI。

砍掉 AI 之後,日子就好過了嗎?沒有,只是坑的種類變了——從「AI 亂猜」變成「我們自己的盲區」。挑幾個講:

**第一個:編譯通過了,畫面是壞的。** 有一次表單容器裡放的控制元件不合格,`ng build` 一路通過,執行起來直接報錯。從此我們學到「**編譯通過 ≠ 畫面是對的**」,加了最後那道無頭瀏覽器實開驗證。順帶一提,第一版修法是用 CSS class 把補救用的隱藏欄位藏起來,結果實測根本沒藏住,畫面多兩個空框——連「修 bug 的修法」都要拿真瀏覽器驗過。

**第二個:東西無聲消失。** 就是剛剛第 3 節破梗的那個案例:整張表格從產出的頁面裡蒸發——沒有報錯,就是安靜地不見了。追下去發現是淘汰判斷靠名字比對,而名字比對的命中率是 0.03%。這類「無聲丟棄」前前後後抓了至少六次,現在是全專案最忌諱的一類 bug:寧可畫面上出現一個誠實的佔位框,也不准任何東西安靜地不見。

**第三個:有產出 ≠ 有畫面。** 時間軸元件,程式碼有產、元件對應也對,但初始資料是空陣列,元件的作法是「沒資料就什麼都不畫」——於是畫面一片空白,系統還回報一切正常。教訓:凡是「沒資料就整個看不見」的元件,預覽假資料不是加分項,是必需品。

**第四個,我最喜歡講的:自信的捏造。** 清舊程式碼時發現一個彈窗的按鈕文字被寫死成「取消」「確認」——看起來完全合理對吧?對照設計稿,真實按鈕是「**林小可**」和「**前往待辦案件**」。「取消/確認」是憑感覺編出來的。這就是 AI 生成(和人憑印象寫碼)最危險的地方:**錯得很合理**。這也是為什麼我們最後選擇:做不到就明說做不到,絕不產一個「看起來對」的東西。

---

# 第 5 節 可以帶走的模式(10 分)

## 投影片要點

**投影片 5-1:六個可復用的模式**

| # | 模式 | 一句話 |
|---|---|---|
| 1 | **確定性優先,AI 放邊緣** | 能寫成規則的絕不用 AI;AI 只放在「錯了也無害」的位置(修編譯錯、給建議、取名字) |
| 2 | **不猜,留洞** | 資訊不夠就留一個結構化的洞問人,附上已知的部分。錯的猜測比誠實的空白貴十倍 |
| 3 | **找一個不會說謊的裁判** | 我們的裁判是 `ng build`。任何流程都該有一個機器可驗、不受人為判斷影響的過關條件 |
| 4 | **裁判也要驗**:編譯通過 ≠ 畫面是對的 | 多一層真實環境驗證(無頭瀏覽器實開頁面) |
| 5 | **閉環,而不是報表** | 問題清單如果不能「處理完就消失、下次不再出現」,就只是噪音。收件匣一鍵對應、答案入庫重放、回報的 bug 變成回歸測試 |
| 6 | **用身分證字號,不用名字** | 對照關係釘在穩定的 key 上,不釘在會變的名字上。人做的決定才能沉澱 |

**投影片 5-2:兩句原話**

> 「錯 mapping = 自信的垃圾,比誠實的佔位框更糟。」
>
> 「身分層會收斂(元件庫是有限的);結構層是無界前線(組裝自由度無限)。戰略要從『吸收每種習慣』轉成『縮小要猜的面』。」

## 講稿

這一節講「你們可以帶走什麼」。這套系統綁著我們的元件庫,程式碼搬不走,但**模式**搬得走,不管你們用什麼框架、做什麼工具。

**第一,確定性優先,AI 放邊緣。** 我們沒有反 AI——系統裡今天還有五、六個地方在用 AI:修編譯錯誤、幫 manifest 建議填值、分析錯誤、幫頁面取語意化的檔名、還有等一下會講的「實作」改碼功能。共同點是:這些位置**錯了也無害**——AI 建議的值你可以不採,AI 修編譯有裁判把關。但「產生程式碼」這條主線,AI 一步都不碰。判斷標準很簡單:這一步需要的是「判斷」還是「規則」?是規則,就寫成程式;寫不成規則的,才考慮 AI,而且要問「它錯了會怎樣」。

**第二,不猜,留洞。** 資訊不夠的時候,最誘人的選項是「先猜一個合理的」。我們的經驗是:猜錯的成本遠高於留白,因為錯得合理的東西**會通過審查**,一路活到使用者面前。留洞不是擺爛,洞是結構化的:附上已經知道的部分(欄位標題、樣本值),人只填缺口,填過的答案入庫,下次自動帶入。

**第三,找一個不會說謊的裁判。** 我們所有的爭論最後都交給 `ng build`:編得過就是過,編不過就是不過,它不看誰的面子。做任何自動化流程,先問自己:我的裁判是誰?如果過關條件是「看起來不錯」,那這條流程遲早失控。

**第四,裁判也要驗。** 剛講過的故事:編譯通過、畫面卻是壞的。單一裁判有它的盲區,所以我們在後面又加了一層真實瀏覽器實開。

**第五,閉環,而不是報表。** 這是我覺得對設計師最有感的一條。健檢工具人人會做,難的是:問題處理完會不會**自己消失**?會不會**下次不再問**?我們的規矩是:未識別元件對應一次、全系統受惠;設計師答過的問題永遠不再出現;使用者回報的 bug 修好之後自動變成一條回歸測試,同樣的問題從此再也回不來。**單調收斂**——清單只會變短,不會變長——是所有這類工具該有的性質,不然它就只是另一個沒人看的報表。

**第六,用身分證字號,不用名字。** 任何「對照表」——設計元件對程式元件、欄位對資料源——都要釘在穩定的識別碼上。名字是給人看的,它會改;人花時間做的決定如果釘在名字上,名字一改就全部作廢。

---

# 第 6 節 設計稿要怎麼畫,系統才吃得下(12 分)

## 投影片要點

**投影片 6-1:先講好消息——「不用做」的事**

- 規格標註(`[spec]`、標註元件)**不用刪**,系統自動略過
- toast 示意**不用刪**,自動剝除
- 頁首/選單/麵包屑畫不畫都可以,系統自動歸位成標準外殼
- 元件命名的機械式差異(空格、大小寫、尺寸後綴)系統自動吸收,**不會來煩你**
- 純動作按鈕(送出/取消)不用接 prototype 線

**投影片 6-2:交稿前檢查清單(核心 10 條)**

1. 元件一律用團隊發佈的**元件庫**拉,**不要 detach**(拆散)——這是第一戒律
2. 表格用 table 元件,不要拿一顆顆 cell 手拼
3. 一個 frame = 一頁;桌面頁寬 **1440**
4. 排版用 **Auto Layout** + 系統級距(8/16/24…)——手動拖的位置會被忽略
5. 彈窗:遮罩層命名 `backdrop` + 面板命名 `dialog`(或 `drawer`),面板內要有 `content`/`body`
6. 真頁面名稱避開隔離關鍵詞(`[spec]`、草稿、draft、NG、封面、元件庫…)——這些詞本來就是給規格板、草稿這類「不要生成」的板子用的,用在那裡是好幫手,只是別讓真頁面撞名
7. 頁面最上方留 header
8. 會跳頁、開彈窗的按鈕,拉 **prototype 連線**
9. 多狀態頁(頁籤、步驟):每個狀態畫一張完整頁,整組拖進**同一個 Section**(Shift+S),Section 名 = 頁名
10. 文字用真文字圖層、icon 用元件庫的 icon

**投影片 6-3:為什麼 detach 是第一戒律**

- detach 之後元件失去身分證字號(key),系統**連「認不得」都不知道**——它會被當成一堆普通方塊靜默降級,不會有任何提示
- 真實掃描數據(2026-08-04,元件庫模板檔 10 頁):748 個問題,扣掉本來就不該生成的板子,真問題 366 個;**145 個嚴重問題裡的 121 個(83%)集中在兩處 detach**,而且都在元件庫模板內部——**修模板一次,所有新專案受惠**
- 同一次掃描裡有一頁 **0 問題**——證明規則做得到,不是標準太嚴

**投影片 6-4:畫面上三種顏色的框,分別該誰處理**

| 顏色 | 意思 | 誰處理 |
|---|---|---|
| 灰色虛線 | 未識別元件 | 產生的人在收件匣一鍵對應 |
| 琥珀色 | 已淘汰元件 | 設計師換成現役版 |
| 藍色 | 已交辦待開發 | **不用管**,工程師補完自動消失 |

## 講稿

這一節是給設計師的實用段。先說好消息:**要求比你想像的少**。我們的原則從第一天就是「設計師約束最小化,但生成穩定性不退讓」——能用程式自動吸收的,絕不變成設計師的規矩。所以規格標註不用刪、toast 示意不用刪、命名的大小寫空格差異系統自己吞。真正要記的就是檢查清單這十條,其中最重要的是三件事:

**第一件事,也是第一戒律:用元件庫,不要 detach。** 為什麼這條這麼重要?回到「身分證字號」:元件庫拉出來的元件帶 key,系統認得;就算是還沒對應過的新元件,系統至少知道「這裡有個我不認識的元件」,會給你一個誠實的灰框。但 detach 之後 key 就沒了——系統連「不認識」都無從知道,那塊就會被當成普通方塊悄悄降級,**沒有任何提示**。它是所有問題裡唯一「壞得無聲無息」的,所以排第一。

再給大家一個其實很樂觀的數字。我們掃過元件庫模板檔的十個模板頁,真問題 366 個,聽起來很多對吧?但拆開看:嚴重問題 145 個,**其中 121 個集中在同兩個地方的 detach,而且都在模板內部**。意思是:這不是「大家畫得不好」,是模板本身有兩個地方要修——修一次,以後照模板畫的每個新專案都直接受惠。而且同一批掃描裡有一整頁是零問題的,證明這些規則完全做得到。

**第二件事:Auto Layout。** 系統讀的是 Auto Layout 的結構,**手動拖的座標會被直接忽略**。一排八個欄位如果是用拖的排出來的,產出來可能變成一直行。這對很多設計師是新聞,值得在這裡講明白:在這套系統眼裡,「看起來排得很整齊」和「結構上排得很整齊」是兩回事,它只信後者。

**第三件事:用「宣告」代替讓系統猜。** 兩個例子。彈窗:遮罩命名 `backdrop`、面板命名 `dialog`,系統就明確知道這是彈窗,而不是從長相去猜。多狀態頁:頁籤或步驟流程,每個狀態畫一張完整頁,整組拖進同一個 Section——這個動作就是在跟系統說「這幾張是同一頁的不同狀態」。我們曾經做過「頁籤文字一樣就自動歸成一組」的猜測邏輯,後來發現打錯一個字就拆錯,現在猜測只當備援,宣告才是正路。全部要記的保留字只有四組:`backdrop`/`dialog`、`content`/`body`、`header`/`footer`、`panel_container`。

最後,拿到產出的頁面後,看到三種顏色的框不用慌,它們是系統在誠實說話:灰框是「我不認識這個,請在收件匣告訴我」;琥珀框是「這個元件已淘汰,請換現役版」;藍框是「已經交辦給工程師了,你什麼都不用做」。還有,不確定稿子畫得合不合規範,不用真的跑一次產生——「先檢查設計稿」按鈕貼上網址就給你完整健檢,附可以直接點回 Figma 的連結。

---

# 第 7 節 五個大哉問——事先收集的問題(12 分)

> 這一節的原則:**能就說能,不能就誠實說不能、說明理由和困難點。** 每題投影片一頁,結構統一:「做得到的」/「做不到的(和為什麼)」。

## 問題 1:切版模式是一頁一頁的靜態版面,還是能串聯、有互動的頁面?

**答案:不是靜態切版。三層互動都是真的,但有明確的邊界。**

做得到的:

1. **元件層互動,原生就有**——因為產出用的是真的元件庫元件,不是畫出來的樣子。下拉選單會展開、日期選擇器會出面板、必填欄位會亮錯誤提示,這些是元件本身的行為,不用任何額外處理。
2. **頁面之間的跳轉,自動接線**——設計師在 Figma 拉的 prototype 連線,系統會讀出來,轉成真的路由跳轉程式碼:按鈕點下去真的換頁。多頁批次產生時,所有頁面進同一個專案,路由和側邊選單自動接好,並且是「整份重生」——重跑同一頁不會長出重複的選單項。
3. **開彈窗,自動接線**——按鈕連到彈窗的 prototype 連線,轉成真的開窗程式碼。而且順序無關:先產頁後產彈窗、或反過來,都接得起來;目標還沒產生時留一個安全的待接記號,絕不會因此編譯失敗。

做不到的(誠實講):

- **業務邏輯不會自動出現。**「按下查詢後表格要顯示什麼、什麼條件下欄位要變化」——這些資訊 Figma 裡根本沒有,任何工具宣稱能從設計稿生出業務邏輯,都是在猜。我們的作法:資料接哪裡用 manifest 問你,更複雜的行為用「實作」功能以白話描述、讓 AI 在安全機制下改碼(見問題 3)。
- **沒接 prototype 線的按鈕就是不跳。** 我們曾評估過用「按鈕文字比對」去猜跳轉目標,拍板不做——模糊比對可能接錯,**寧缺勿錯**。

## 問題 2:能切出下拉選單、彈跳視窗這類元件互動嗎?

**答案:可以,而且是今天支援得最完整的部分之一。**

做得到的:

1. **下拉選單**:有完整食譜,產出真的 select 元件加選項;選項值直接從設計稿採收(連收合狀態下藏起來的選項面板都會去讀),必填的自動掛驗證。
2. **自動完成(autocomplete)**:不只產面板,連「打字即時篩選」的邏輯都產好——這是我們特別補的,不然面板永遠列出全部選項。
3. **日期選擇器**:輸入框 + 日曆按鈕 + 彈出面板,整組接好。
4. **彈跳視窗**:支援,但要理解它的定位——彈窗在我們系統裡**不是一個元件,是一整張 frame 的身分**。設計師照規範畫(`backdrop` + `dialog` 命名),那張 frame 就會被產成一個獨立的彈窗元件,加上開窗按鈕的自動接線。抽屜(drawer)同理。
5. 其他有食譜的互動元件:頁籤、手風琴、步驟條、氣泡提示、下拉選單按鈕、分頁(內建在表格裡)等;全部食譜合計 29 份。

做不到的(誠實講):

- **彈窗有畫法門檻**:遮罩 + 面板的結構、面板內要有內容區。畫錯的彈窗以前會被整張吞掉,現在會在健檢裡明確告訴你哪裡不合。
- **toast(短暫提示)不生成**——它是頁面跑起來之後才臨時跳出來的東西,不是頁面結構的一部分;設計稿上的 toast 示意會被自動剝掉,這是刻意的。
- **沒有食譜的元件不會硬生**:留佔位框 + 進待辦。目前的機制是工程師補一份食譜,該元件從此全系統可用。

## 問題 3:後續頁面維運怎麼進行?

**答案:產完之後有三條路,各管一種改動;但「交付給工程團隊之後的長期維運」還沒有定案,誠實說這是目前最弱的一環。**

做得到的:

1. **設計稿改了 → 重新產生。** 重新產生是安全的重複動作(工程上叫「冪等」):同一頁重跑會乾淨地覆蓋,路由和選單整份重生,不會累積垃圾。
2. **資料要換 → 改 manifest。** 資料層的改動不碰程式碼,重填表單即可,答過的答案有記憶。
3. **細節要調 → 「實作」功能(implement)。** 這是系統的第二條路:挑一個已產生的專案,用白話描述需求(「把這兩欄對調」「加一個確認步驟」),AI 直接改那個專案的程式碼。安全機制設計得很保守:**AI 改的永遠是專案的複本,編譯通過才換回正本,沒通過就整份丟掉**——你的專案在整個過程中一個位元組都不會被動到。

誠實講困難點:

- **兩條路會互相覆蓋。** 用「實作」改過的頁面,一旦重新產生就會被覆蓋回設計稿的樣子。系統會在專案上蓋一個「有 AI 改動」的章提醒你,也用專案鎖擋住「一邊產生一邊改碼」的並發衝突,但「改動合併」這件事本質上沒有漂亮的解法——設計稿和手改是兩個各自為政的真相來源。目前的建議做法:以設計稿為主幹,「實作」的改動視為「重新產生後要再描述一次」的差異。
- **產出交付之後的生命週期還沒建立。** 目前系統定位是「從設計稿到可跑頁面」的生產起點;產出的頁面如何進入正式產品的版本控制、由工程團隊接手長期演進、之後設計稿再改版怎麼同步——這一段流程還沒有定案,是未來方向(第 8 節)。今天不假裝這題已解。

## 問題 4:對設計稿本身有什麼要求?layout、命名需要一套準則嗎?

**答案:需要,而且我們已經有一套成文的準則——但它比想像的溫和,且每一條都有數據支撐,不是憑感覺訂的。**

(內容即第 6 節,這裡投影片只放摘要,口頭帶過並回指第 6 節。)

補充兩點給工程師聽:

1. 這套規則不是拍腦袋:我們掃了 12 個真實設計檔、1,662 個頁面候選,每條規則都有「違反了會發生什麼」的統計證據;有兩條規則因為數據不支持被我們自己砍掉了(其中一條若真的上線,會誤殺約 88% 的合法頁)。**規則也要有裁判。**
2. 準則有三個等級:擋下不生成(會產生靜默垃圾的)、警告但照跑(會降級的)、純建議。而且健檢工具跟產生器**共用同一套判斷程式**,不會出現「健檢說沒事、產生卻失敗」的分裂。

## 問題 5:未來有規劃開放名單給各子公司使用這個平台嗎?

**答案:誠實說——目前沒有這項規劃。分發已經做到一部分,但「平台化開放」是另一個量級的事,而且不純粹是技術問題。**

現況做到的:

1. **Windows 內網安裝包已經出貨**:IT 在共享磁碟放一包,使用者雙擊安裝、輸入一次 Figma 的存取金鑰(token),之後雙擊就開。不需要 Git、不需要 Docker、不改系統設定,砍掉資料夾就是乾淨移除。所以「同一個網域內多發幾台機器」今天就做得到。
2. 安裝版沒有內建 AI(AI 引擎程式要各自授權,內網環境也不允許隨包散布),AI 相關按鈕會自動收起——但**核心產碼本來就沒有 AI,所以核心價值一點都沒少**。

誠實講困難點(為什麼「開放平台」不是現在的下一步):

1. **架構是單機單使用者**:後端只聽本機、沒有帳號、權限、租戶隔離,Figma 存取金鑰是每台機器自己的。要開放成多人共用的平台,等於補一整層帳號授權系統,是重工程。
2. **系統深度綁定我們這套元件庫**:29 份食譜、85 筆元件登記、348 筆身分對照,全部是針對這套元件庫建的。子公司如果用同一套元件庫,遷移成本低;用不同的設計系統,等於要重建整套知識庫——可複製的是**方法**,不是資料。
3. **維運與支援量能**:設計稿健檢、未識別元件收件匣這些閉環,都預設有人在顧。開放前要先回答「誰支援」。
4. 還有一些收尾:安裝版目前只驗到「編譯通過」、少了畫面實開那層驗證;公司實機的端到端驗收也還在進行。

所以正確的說法是:**「分發」技術上已可行,「開放平台」未規劃**——後者是組織決策,不是我們今天單方面能承諾的。如果方向確定,上面這四點就是工作清單。

## 講稿(本節開場白)

接下來回答大家事先丟過來的五個問題。我先講我回答的原則:**能做到的說能,做不到的直接說做不到、講清楚為什麼難**。這套系統本身的設計哲學就是「不猜、不假裝」,我今天回答問題也用同一套哲學。(逐題講,內容如上。)

---

# 第 8 節 未來方向 + Q&A(6 分+)

## 投影片要點

**投影片 8-1:接下來的方向(按確定程度排)**

1. **首見元件的 AI 橋(進行中)**:遇到沒有食譜的新元件時,用 AI 起草一份食譜、**人審核後**落地成永久規則——AI 只出第一版草稿,審過之後就是確定性的一部分。「AI 只見第一面,之後交給規則」。
2. **設計稿源頭治理**:元件庫模板檔的問題修在源頭(83% 的嚴重問題集中在兩處),讓新專案天生乾淨。
3. **交付流程**:產出頁面進入正式版控、工程團隊接手的路徑(第 7 節問題 3 承認的缺口)。
4. **響應式**:目前只支援 1440 桌面版型,斷點變體是未來題。
5. 安裝版補畫面實開驗證、公司實機驗收收尾。

**投影片 8-2:結尾一頁(重複開場那句話)**

> 產生的過程零 AI;AI 放在錯了也無害的邊緣。
> 認不得就誠實說認不得——**自信的錯誤,比誠實的空白更糟。**
>
> 謝謝大家。問題歡迎現在提,也歡迎會後丟過來。

## 講稿

最後講未來。最確定的一條是「首見元件的 AI 橋」:現在遇到沒有食譜的元件要等工程師手寫,未來想讓 AI 起草第一版食譜、人審核後落地。注意這仍然守著同一條底線——AI 只出草稿,**審核過後它就變成確定性規則的一部分**,不是每次生成都讓 AI 即興發揮。我們把這個哲學叫「AI 只見第一面」。

第二條是把設計稿的問題修在源頭:剛剛講過,八成的嚴重問題集中在模板檔的兩個地方,修模板比修一百張稿有效。第三條是誠實面對維運那題:產出的頁面怎麼交付給工程團隊長期演進,這段流程要補。再來是響應式——今天只有 1440 桌面版,這是明確的已知限制,不是忘了。

回到開場那句話作結:這個專案兩個月來最大的心得,不是「AI 能做什麼」,而是**「AI 該放在哪」**——放在錯了也無害的邊緣,把需要穩定的主線交給規則,認不得的誠實說認不得。自信的錯誤,比誠實的空白更糟。謝謝大家,開放提問。

---

# 附錄 A:時間伸縮指南

- **只有 60 分鐘**:Demo 縮成 8 分(砍掉看程式碼那段)、第 4 節只講「10 支 AI 為什麼砍」+ 痛點挑 2 個(通過編譯但畫面壞、自信的捏造)、第 5 節挑 4 個模式(1/2/3/5)、第 7 節每題只講「做得到/做不到」的結論句。
- **有 90 分鐘**:照本大綱全講,Q&A 自然吸收剩餘時間。
- **聽眾工程師比例高**:第 3 節加講 emit-lint(寫檔前自檢:絕不把自己的缺陷留給編譯器吐天書錯誤、再叫 AI 收拾)與 golden corpus(11 個真實案例的逐位元組快照迴歸網,0.2 秒現形);第 4 節可加講「同一段邏輯抄成多份必然漂移」與「平台分支在另一個平台上是零覆蓋率」兩課。
- **聽眾幾乎全設計師**:第 3 節縮到 10 分,把時間挪給第 6 節,現場開一張他們熟的設計稿跑「先檢查設計稿」。

# 附錄 B:數字速查表(2026-08-07 實測)

| 數字 | 是什麼 |
|---|---|
| 29 | 元件食譜數(controls 12 / display 9 / layout 7 / data 1) |
| 85 | 元件登記表條目 |
| 348 | key 身分對照條目 |
| 117 | 人工決定(seed overrides)累積筆數 |
| 113 | 設計師自己在前端答完的決策題(2026-07-31 結案) |
| 1,380 | 後端自動測試(全數通過;從砍掉 AI 鏈那天的 203 條一路長上來) |
| 11 | golden corpus 迴歸案例(頁/頁籤/步驟/彈窗都有) |
| 12 檔 / 1,662 頁 | 黃金規則的統計證據來源(真實設計檔全量掃描) |
| 16 秒 | 一次產生的中位數時間(p90 約 7.6 分,長的都是在等人填 manifest) |
| 83ms | 核心產碼本身跑完 9 頁的時間(瓶頸從來不是產碼,是編譯) |
| 0.03% | 用「名字」判斷淘汰元件的召回率(38,021 中 13 個)——key 對照的存在理由 |
| 36 → 1 | AI 猜版面插槽的災情(猜 36 個,正確 1 個)——砍 AI 鏈的導火線之一 |
| 748 → 366 → 121/145 | 模板檔健檢:原始問題 → 真問題 → 嚴重問題中集中在兩處 detach 的數量(83%) |
| 850 | 產碼流程中的特例規則總數(45% 集中在「讀設計稿」那段——不整齊都在進門處被接住) |
| 10 | 編譯失敗後 AI 自動修復的輪數上限 |

# 附錄 C:講者備忘(可能被追問的點)

1. **「為什麼不用 Figma 官方的 Dev Mode / Code Connect?」** 早期用過 Code Connect,2026-06-24 換成自建 key 對照表(commit 記錄在案)。核心原因:我們要的是「整頁確定性產出 + 認不得就留洞」的完整閉環,不只是元件對應。
2. **「AI 修編譯,不會愈修愈壞嗎?」** 有三道防線:上限 10 輪;每輪前後對產出目錄拍快照、錯誤指紋記進日誌(同一指紋反覆出現 = 該把對應食譜改硬,而不是每次叫 AI 擦);機器上沒裝 AI 引擎時上限自動歸零,直接誠實回報失敗。
3. **「猜測都不做,會不會太死板?」** 我們保留極少數「寧缺勿錯」的猜測備援(如頁籤文字比對),但方向一律是宣告優先。反例教訓在案:曾把「看起來像三欄」自動改成三欄的提案否決掉——相信畫出來的寬度等於讓確定性系統開始猜意圖,只報不修。
4. **「113 題設計師決策是什麼?」** 元件庫裡有些元件光看名字判斷不了身分(是獨立元件?某元件的零件?已淘汰?),系統把這些整理成封閉選擇題,設計師在網頁上逐題選答(每題附 Figma 深連結),答過永久生效、不再重問。整批 113 題已於 7 月底全數答畢——而且是設計師自己操作前端完成的,順帶驗證了設計師用得動這套介面。
5. **「產出的程式碼品質誰把關?」** 四層:寫檔前自檢(emit-lint,含元件屬性值域檢查——240 個元件的合法屬性值是從型別定義檔機器掃出來的)、prettier 排版、`ng build`、無頭瀏覽器實開。外加 11 案例的快照迴歸網釘住既有輸出。
6. **「無聲丟棄當初是怎麼發生的?」** 它是一類 bug,不是一個 bug。共同結構:系統本來就有「合法的丟棄路徑」(剝外殼、忽略裝飾圖形),**錯的東西走進了合法丟棄的門,而那扇門當時不記帳**。三類成因,各附真實案例:
   - **身分訊號在源頭斷了**(設計端,今天仍可能):detach 讓元件失去 key,系統連「認不得」都無從知道,整塊靜默降級成一堆普通方塊——這是唯一還會發生的來源,也是「detach 是第一戒律」的原因;早期用名字判「已淘汰」,38,021 個實例只抓到 13 個(0.03%),漏網的整張表格蒸發。
   - **合法丟棄的判準太寬**(系統端,已修):子字串比對誤殺(`it info` 剛好是 `credit info` 的尾巴,Credit Info 整塊被當外殼剝掉);剝殼停錯層,把頁面的固定操作列當外框丟掉(事後立準則:剝殼只准丟外框,不准丟內容);外殼剝除整棵刪但不記帳。
   - **退路把失敗偽裝成成功**(系統端,已修):彈窗找不到內容區就「退回整張 frame」,整頁示意稿被吞進彈窗、待辦清單卻是空的(修法:拿掉退路);Figma 伺服器對超大頁面會砍掉部分內容,程式只印警告照常走,產出空區塊、編譯照樣通過。
   - 近親「無聲空白」:沒被丟但畫面全空(時間軸初始資料空陣列,元件沒資料就整個不畫),防法是空狀態必須有預覽假資料。
   - Q&A 一句話版:「每次都是合法丟棄的門開太大。修法永遠是兩件事:**把門的判準改窄,和讓每一次丟棄都留下帳**。」

# 附錄 D:轉換過程四段對照(投影片素材)

> 用一個最小的真實例子講「一個元件是怎麼被轉換的」:一個水平表單欄位,標籤「狀態」+ 一顆下拉選單。
> 所有格式都節錄自真實系統(corpus 案例「基礎查詢列表」),欄位名是真的,內容有精簡;`…` 代表省略、`←` 是給聽眾看的註解,不是檔案內容。
> 建議一頁投影片放一段,共四頁;或左右並排放「前一段 → 後一段」對照。

## 第 1 段:Figma 給我們的原始資料(節錄)

設計師畫的那顆欄位,從 Figma 伺服器抓回來長這樣——重點是:**圖層名字只是變體描述,真正的身分藏在對照鏈的最底層**。

```jsonc
{
  "document": {
    "id": "3052:13893",
    "name": "form field_m",            // ← 圖層名,只是給人看的
    "type": "INSTANCE",                // ← 是元件庫拉出來的實例(有身分)
    "componentId": "5284:42168",       // ← 指向下面 components 表
    "children": [
      { "type": "INSTANCE", "name": "label_m",
        "children": [ { "type": "TEXT", "characters": "狀態" } ] },
      { "type": "INSTANCE", "name": "select_m" }   // ← 下拉選單,設計稿沒填選項
    ]
  },
  "components": {
    "5284:42168": {
      "name": "layout=horizontal, w label=true, …",   // ← 「名字」其實是變體參數!
      "componentSetId": "5284:42165"                  // ← 再指向 componentSets 表
    }
  },
  "componentSets": {
    "5284:42165": {
      "key": "f7f20aa45880af3fd4cb2d1b63e7cb0adf1a9ea1",  // ← 身分證字號(key)在這裡
      "name": "form field_m"
    }
  }
}
```

## 第 2 段:查身分(key 對照表)

拿著 key 去查我們維護的對照表(`key_seed.json`,348 筆)。**只查 key,完全不看名字**——名字會改、會打錯,key 不會。

```jsonc
// key_seed.json(節錄)
{
  "f7f20aa45880af3fd4cb2d1b63e7cb0adf1a9ea1": {
    "name": "form field_m",
    "role": "component",               // ← 這是一個正式元件
    "selector": "cub-form-field"       // ← 對應到元件庫的這個元件
  }
}
// 查不到 key?→ 灰色佔位框 + 進「未識別元件」收件匣,絕不硬猜
```

## 第 3 段:中間結構樹(IR)——蒸餾過的「這是什麼 + 設計給了什麼」

Figma 的雜訊(座標、顏色碼、變體字串)被蒸餾掉,留下乾淨的結構:每個節點記「它是什麼元件、設計稿給了哪些資訊、它從哪裡來」。這一層就是「0 無聲丟棄」點名發生的地方——每個節點都有 `kind`,連被忽略的都有(`"kind": "ignore"`)。

```jsonc
{
  "kind": "component",
  "selector": "cub-form-field",
  "inputs": { "orientation": "horizontal" },        // ← 從變體參數讀出:水平排列
  "provenance": {                                   // ← 出處帳:它是誰、怎麼認出來的
    "figmaId": "3052:13893", "name": "form field_m", "confidence": "seeded"
  },
  "children": [
    { "kind": "component", "selector": "cub-label",
      "children": [ { "kind": "text", "content": "狀態" } ] },
    { "kind": "component", "selector": "cub-select",
      "inputs": { "placeholder": "請選擇" },         // ← 提示文字從設計稿採收
      "children": [] }                               // ← 設計稿沒填選項 → 這裡沒有 options
  ]
}
```

## 第 4 段:照食譜煮出 Angular 碼

`cub-form-field` 和 `cub-select` 各有一份食譜(固定規則,不是 AI)。注意兩件事:選項沒資料 → 給**預覽假資料**讓畫面先動起來;同時**開一個資料洞**問人真實選項是什麼。

```html
<!-- 狀態.component.html(節錄) -->
<cub-form-field orientation="horizontal">
  <cub-label widthMode="fixed" customWidth="32px">狀態</cub-label>
  <cub-select placeholder="請選擇" [(value)]="field1Value" style="flex: 1 1 0; min-width: 0">
    @for (opt of field1Options; track opt.value) {
      <cub-option [value]="opt.value">{{ opt.label }}</cub-option>
    }
  </cub-select>
</cub-form-field>
```

```ts
// .component.ts(節錄)——import 的全是元件庫的正式元件
import { CubFormField } from 'cub-lib-view-rootng/component/form-field';
import { CubLabel }     from 'cub-lib-view-rootng/component/label';
import { CubSelect }    from 'cub-lib-view-rootng/component/select';
import { CubOption }    from 'cub-lib-view-rootng/component/option';

field1Value: string | null = null;
field1Options: { value: string; label: string }[] = [
  { value: '1', label: '範例一' },
  { value: '2', label: '範例二' },
  { value: '3', label: '範例三' },
]; // 預覽假資料;填真實選項或綁來源
```

```jsonc
// 同時產出的資料洞(contract,節錄)——之後變成 Demo 裡那張問人的表單
{
  "kind": "options",
  "member": "field1Options",
  "needs": "這個下拉選單的真實選項",
  "known": { "label": "狀態", "placeholder": "請選擇" },   // ← 設計稿已給的部分
  "type": "{ value: string; label: string }[]"
}
```

## 講稿(走這四段時)

這一頁我們把魔術拆開,看一個欄位是怎麼一路變成程式碼的。

第 1 段,Figma 給我們的原始資料。注意一件反直覺的事:這個圖層叫 `form field_m`,但你往 components 表裡看,「名字」那一欄寫的是 `layout=horizontal, w label=true`——在 Figma 的世界,名字常常只是變體參數,不是身分。真正的身分要一路查到 componentSets 表最底下那一串亂碼——那就是 key,元件的身分證字號。這就是為什麼「用名字認元件」註定失敗,我們早期真的踩過。

第 2 段,拿 key 去查我們的對照表。查到了,它是 `cub-form-field`。查不到呢?不猜——灰框、進收件匣、問人。

第 3 段,中間結構樹。可以把它想成「蒸餾」:Figma 原始資料裡九成的東西(座標、顏色碼、變體字串)對「這是什麼元件」沒有幫助,蒸餾完只留三件事——它是什麼(selector)、設計稿給了什麼(排列方向、提示文字)、它從哪裡來(出處帳)。剛剛講的「0 無聲丟棄」點名就發生在這一層:每個節點都必須有 `kind`,連被忽略的裝飾圖形都有一筆 `ignore` 的帳。

第 4 段,照食譜煮成程式碼。看最後那個 `field1Options`——設計稿沒填選項,所以系統給了三筆「範例一、二、三」的預覽假資料,讓畫面先能動,然後同時開了一個資料洞,記著「這裡欠一份真實選項,已知它的標籤叫狀態」。這個洞,就是 Demo 裡停下來問你的那張表單的來源。整條鏈走完,沒有任何一步是用猜的。
