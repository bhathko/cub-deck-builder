# codex-imagegen (disabled)

This install is **locked to Codex's native `imagegen` skill only** (company policy:
no non-Codex tools). The `codex exec` / `baoyu-image-gen` routing this file used to
describe has been intentionally removed.

If Codex's native `imagegen` is not available in the current runtime, **report a
blocker** — do not route image generation through `baoyu-image-gen`, a bundled
wrapper, Cursor, or any other tool.
